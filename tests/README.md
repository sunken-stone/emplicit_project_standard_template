# Tests

All test files live here. Follow these rules:

- Name test files to mirror the source file: `app.py` → `test_app.py`
- Never modify the original source file when writing tests
- Use `pytest` for Python, `Jest` for JavaScript
- Write critical path tests only — do not chase coverage percentages
- Only write tests when explicitly asked to
